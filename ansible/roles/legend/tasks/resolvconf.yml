---
- name: Add google DNS to resolvconf
  sudo_user: root
  copy:
    dest: /etc/resolvconf/resolv.conf.d/head
    content: |
      # AUTOGENERATED BY ANSIBLE
      nameserver 8.8.8.8
      nameserver 8.8.4.4

- name: Update resolvconf
  sudo_user: root
  command: resolvconf -u
